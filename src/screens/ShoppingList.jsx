import * as React from "react";
import { View, Text } from "react-native";

const ShoppingList = ({navigation}) => {
    return(
        <View>
            <Text>Voici la liste de courses</Text>
        </View>
    )
}

export default ShoppingList;