import * as React from "react";
import { View, Text } from "react-native";

const AddressBook = ({navigation}) => {
    return(
        <View>
            <Text>Voici le carnet d'adresses</Text>
        </View>
    )
}

export default AddressBook;